# do-nothing
Do nothing.

It's just another noop function

## Usage
```js
const doNothing = require('@((()/do-nothing')
doNothing()  // does nothing
```

## FAQ
### Why?
noop functions are useful
### whaat???
you don't understand it's useful, people would save like two bytes if they all used this package instead of defining noops themselves
### what else?
the problem is there's so many that everyone uses a different one so they're wasting bytes instead. so everyone should use this one. maybe.
### huh?
yeah its pretty weird. you'll understand someday